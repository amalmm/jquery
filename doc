select
$(selector).action()

reusable select 
var select = $("#mySelect");
>select.find("$id").val();
>$select.action()

insert content at end,before,after,prepend
$(selector).append()

add attribute
$(selector).attr()

event handler
$("select").on("action",'element:op',function(){})

